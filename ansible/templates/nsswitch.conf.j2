# /etc/nsswitch.conf
#
# Name Service Switch configuration file.
#

passwd:         files {% if ldap_enabled %}ldap{% endif %} systemd
group:          files {% if ldap_enabled %}ldap{% endif %} systemd
shadow:         files {% if ldap_enabled %}ldap{% endif %}
gshadow:        files

hosts:          files mdns4_minimal [NOTFOUND=return] dns
networks:       files

protocols:      db files
services:       db files
ethers:         db files
rpc:            db files

netgroup:       nis